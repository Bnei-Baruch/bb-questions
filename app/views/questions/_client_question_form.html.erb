<div id="question-cont-<%= question.id %>" class="panel panel-default">
    <div class="panel-heading">
        <%= "#{question.name} " %>
        <%= question.from.blank?? "": "#{ t('activerecord.attributes.question.from')} #{question.from}" %>
    </div>
    <div dir="auto" class="panel-body"><%= question.question %></div>
</div>